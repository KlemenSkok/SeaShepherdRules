#include "Game.hpp"

Game::Game() {
    window = nullptr;
    renderer = nullptr;
}

void Game::run() {
    //main loop

    //check inputs, process events
    //update game state
    //check for collisions
    //render game state
}